<?php

/**
 * Callback for incoming voice calls.
 */
function swsfdemo_twilio_voice_incoming($voice, $options) {
  watchdog('indemo', 'd');

  //watchdog('in', $voice);
  //watchdog('in', print_r($options, TRUE));
}